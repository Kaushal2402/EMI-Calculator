#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "Generated.xcconfig"

// AdMob application ID (SOW §4.8 · TASKS 7.1). Referenced by Info.plist's
// GADApplicationIdentifier. Value below is Google's public test app ID; for a
// release build replace it here or override ADMOB_APP_ID_IOS in CI. Xcode
// treats "//" as a comment, so keep the "~" form (no slashes) in this value.
ADMOB_APP_ID_IOS = ca-app-pub-3940256099942544~1458002511

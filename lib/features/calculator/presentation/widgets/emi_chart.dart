import 'package:flutter/material.dart';

/// Animated fl_chart donut, interest vs principal (SOW §4.5). Built in task 5.2.
///
/// PHASE 0 SCAFFOLD: placeholder that renders nothing.
class EmiChart extends StatelessWidget {
  /// Creates a [EmiChart].
  const EmiChart({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

import 'package:flutter/material.dart';

/// Monthly/Yearly schedule table with sticky header (SOW §4.4). Built in task 5.3.
///
/// PHASE 0 SCAFFOLD: placeholder that renders nothing.
class AmortizationTable extends StatelessWidget {
  /// Creates a [AmortizationTable].
  const AmortizationTable({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

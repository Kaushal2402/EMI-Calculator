import 'package:flutter/material.dart';

/// Segmented control for Home / Car / Personal loan (SOW §4.1). Built in task 4.2.
///
/// PHASE 0 SCAFFOLD: placeholder that renders nothing.
class LoanTypeSelector extends StatelessWidget {
  /// Creates a [LoanTypeSelector].
  const LoanTypeSelector({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

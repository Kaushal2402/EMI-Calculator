import 'package:flutter/material.dart';

/// Tenure TextField + Slider + Yr/Mo toggle (SOW §4.2). Built in task 4.5.
///
/// PHASE 0 SCAFFOLD: placeholder that renders nothing.
class TenureInputField extends StatelessWidget {
  /// Creates a [TenureInputField].
  const TenureInputField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

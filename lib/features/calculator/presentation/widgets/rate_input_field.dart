import 'package:flutter/material.dart';

/// Interest-rate TextField + Slider, 1–36% step 0.05% (SOW §4.2). Built in task 4.4.
///
/// PHASE 0 SCAFFOLD: placeholder that renders nothing.
class RateInputField extends StatelessWidget {
  /// Creates a [RateInputField].
  const RateInputField({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
